import { ToastrHttpInterceptor } from './toastr-http-interceptor';

describe('ToastrHttpInterceptor', () => {
  it('should create an instance', () => {
    expect(new ToastrHttpInterceptor()).toBeTruthy();
  });
});
